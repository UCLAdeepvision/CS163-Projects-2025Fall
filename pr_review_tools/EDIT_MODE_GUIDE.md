# Edit Mode - 编辑模式指南

## 📝 什么是Edit Mode？

Edit Mode允许你在审查PR时手动修改文件，无需在GitHub上来回修改再重新提交。

### 支持的操作
- ✏️ **修改文件**：修复拼写错误、调整格式
- ➕ **添加文件**：补充遗漏的图片或文档
- ➖ **删除文件**：移除不需要的文件
- 🔄 **自动追踪**：所有变化自动检测并记录

### 使用场景
- 修复文章中的拼写错误
- 调整图片路径
- 格式化代码块
- 优化Markdown格式
- 微调文章结构
- 添加遗漏的图片
- 删除多余的文件

## 🚀 快速开始

### 1. 启动Edit Mode
```
审查控制面板 → 点击 "✏️ Start Edit" 按钮
```

### 2. 编辑文件
使用你喜欢的编辑器（VS Code, Sublime, Vim等）：

**支持的操作**：
- ✏️ **修改文件**：编辑 `_posts/` 下的Markdown文件
- ➕ **添加文件**：复制新图片到 `assets/images/`
- ➖ **删除文件**：删除不需要的文件
- 🔄 **组合操作**：同时修改、添加、删除多个文件

### 3. 完成编辑
```
点击 "✓ Finish Edit" 按钮 → 系统自动检测所有变化
```

**自动检测**：
- ✏️ 修改的文件（modified）
- ➕ 新添加的文件（added）
- ➖ 删除的文件（deleted）

**显示示例**：
```
✓ 检测到 5 个文件变化！

✏️  修改: 2 个
➕ 添加: 2 个
➖ 删除: 1 个
```

### 4. 合并
```
点击 "✅ 接受并合并" → 所有变化会包含在合并中
```

## 🔍 详细工作流

### Phase 1: 准备
**触发**：点击 Start Edit

**系统操作**：
1. 获取当前可编辑文件列表
2. 显示Edit Mode面板
3. 记录初始Git状态
4. 弹出文件列表提示

**界面变化**：
- 显示蓝色Edit Mode面板
- Start Edit按钮消失
- Finish Edit按钮激活

### Phase 2: 编辑
**用户操作**：
```bash
# 方式1：命令行编辑器
cd /path/to/project
vim _posts/2025-12-13-article.md

# 方式2：VS Code
code _posts/2025-12-13-article.md

# 方式3：任何编辑器
open -a "Sublime Text" _posts/2025-12-13-article.md
```

**支持的修改**：
- ✅ 修改文件内容
- ✅ 添加新文件（在允许的目录）
- ✅ 修改现有文件
- ❌ 删除文件（不推荐）
- ❌ 修改其他目录（会被忽略）

### Phase 3: 检测
**触发**：点击 Finish Edit

**系统操作**：
```bash
# 1. 获取当前Git状态
git status --porcelain

# 2. 过滤有效文件
# 只保留 assets/ 和 _posts/ 的修改

# 3. 记录到状态
edited_files = [...]
```

**界面更新**：
```
修改的文件：
✓ _posts/2025-12-13-article.md
✓ assets/images/team30/new-image.png
```

### Phase 4: 合并
**触发**：点击 接受并合并

**系统操作**：
1. 添加所有修改：
   ```bash
   git add _posts/2025-12-13-article.md
   git add assets/images/team30/new-image.png
   ```
2. 提交更改
3. 合并到main
4. 发布GitHub评论（包含编辑记录）

## 📋 GitHub评论示例

```markdown
## 🤖 Automated Review Report

### ✅ Accepted Files (6)
- `_posts/2025-12-13-team30-article.md`
- `assets/images/30/image1.png`
- `assets/images/30/image2.png`
- ...

### ❌ Rejected Files (2)
_These files are not in the allowed directories_
- `Gemfile.lock`
- `_site/index.html`

### ✏️ Edited Files (2)
_These files were modified during the review process_
- `_posts/2025-12-13-team30-article.md`
- `assets/images/30/image1.png`

## 💡 Additional Comments
Fixed typos in the title and adjusted image paths.
```

## 🎯 常见编辑任务

### 修复拼写错误

**场景**：文章标题或内容有拼写错误

**操作**：
1. Start Edit
2. 打开文件：
   ```bash
   code _posts/2025-12-13-article.md
   ```
3. 修正拼写错误
4. 保存文件
5. Finish Edit
6. 在Additional Comment中说明：
   ```
   Fixed typo: "machien learning" → "machine learning"
   ```

### 调整图片路径

**场景**：图片路径不正确，预览显示不出图片

**原文**：
```markdown
![Image](../images/team30/pic.png)
```

**修改为**：
```markdown
![Image]({{ '/assets/images/team30/pic.png' | relative_url }})
```

**操作**：
1. Start Edit
2. 编辑Markdown文件
3. 修改所有图片路径
4. Finish Edit
5. Additional Comment：
   ```
   Fixed image paths to use Jekyll's relative_url filter
   ```

### 替换图片

**场景**：图片质量不佳，需要替换

**操作**：
1. Start Edit
2. 备份原图（可选）：
   ```bash
   mv assets/images/30/old.png assets/images/30/old.png.bak
   ```
3. 复制新图：
   ```bash
   cp ~/Downloads/new-image.png assets/images/30/old.png
   ```
4. Finish Edit
5. Additional Comment：
   ```
   Replaced low-quality image with higher resolution version
   ```

### 格式化代码块

**原文**：
````markdown
```
def hello():
print("Hello")
```
````

**修改为**：
````markdown
```python
def hello():
    print("Hello")
```
````

**操作**：
1. Start Edit
2. 添加语言标识和修正缩进
3. Finish Edit

### 添加缺失的图片

**场景**：PR中引用了图片但忘记上传

**操作**：
1. Start Edit
2. 将图片复制到正确位置：
   ```bash
   cp ~/Downloads/missing-image.png assets/images/30/
   ```
3. Finish Edit
4. 系统检测结果：
   ```
   ➕ [添加] assets/images/30/missing-image.png
   ```
5. 新文件会自动包含在合并中

### 删除多余的文件

**场景**：PR包含了不需要的文件或重复的资源

**操作**：
1. Start Edit
2. 删除不需要的文件：
   ```bash
   rm assets/images/30/duplicate-image.png
   # 或使用文件管理器删除
   ```
3. Finish Edit
4. 系统检测结果：
   ```
   ➖ [删除] assets/images/30/duplicate-image.png
   ```
5. 删除操作会自动包含在合并中

### 组合操作示例

**场景**：修复文章、添加图片、删除重复文件

**操作**：
1. Start Edit
2. 同时进行多个操作：
   ```bash
   # 修改文章
   vim _posts/2025-12-13-article.md
   
   # 添加新图片
   cp ~/Downloads/new-image.png assets/images/30/
   
   # 删除重复图片
   rm assets/images/30/old-duplicate.png
   ```
3. Finish Edit
4. 系统检测结果：
   ```
   ✓ 检测到 3 个文件变化！
   
   ✏️  修改: 1 个
   ➕ 添加: 1 个
   ➖ 删除: 1 个
   ```
5. 所有变化都会包含在合并中，并在GitHub评论中分类显示

## ⚠️ 注意事项

### DO's ✅
- **预览后再编辑**：先在Docker预览中查看问题
- **小改动**：Edit Mode适合小幅修改
- **添加说明**：在Additional Comment中解释修改原因
- **测试**：编辑后在预览中确认效果
- **保持专注**：只修改必要的内容

### DON'Ts ❌
- **大规模重构**：不适合Edit Mode，应该让作者重新提交
- **修改其他目录**：会被忽略
- **删除文件**：不推荐，可能导致问题
- **修改PR作者的意图**：保持原意，只修正错误
- **忘记Finish Edit**：必须点击才能检测修改

## 🔧 高级技巧

### 使用Git查看修改

```bash
# 查看所有修改
git status

# 查看具体修改内容
git diff

# 查看某个文件的修改
git diff _posts/2025-12-13-article.md
```

### 撤销编辑

**在Finish Edit之前**：
```bash
# 撤销所有修改
git reset --hard

# 撤销特定文件
git checkout -- _posts/2025-12-13-article.md
```

**在Finish Edit之后、合并之前**：
- 点击 "⏭️ 跳过" 按钮
- 系统会重置所有更改

### 分阶段编辑

1. **Start Edit**
2. 修改第一批文件
3. **Finish Edit** 查看检测结果
4. 如果需要继续修改：
   - 继续编辑（无需再次Start Edit）
   - 再次点击 **Finish Edit**
   - 系统会更新修改列表

### 批量编辑

使用脚本批量修改：

```bash
# 批量替换图片路径
find _posts -name "*.md" -exec sed -i '' 's|../images/|{{ "/assets/images/" \| relative_url }}|g' {} +

# 批量添加代码块语言标识
# 使用你喜欢的脚本工具
```

## 🐛 故障排除

### 修改未被检测

**症状**：Finish Edit后显示"未检测到文件修改"

**原因**：
- 文件在不允许的目录
- 文件已提交（在Start Edit之前）
- Git忽略了该文件（.gitignore）

**解决**：
```bash
# 检查文件状态
git status

# 确保文件在 assets/ 或 _posts/
ls -la _posts/
ls -la assets/
```

### 修改丢失

**症状**：接受PR后修改未包含在合并中

**原因**：忘记点击 Finish Edit

**预防**：
- 养成习惯：Start Edit → 编辑 → Finish Edit → 接受
- 查看界面上的"修改的文件"列表

### 冲突

**症状**：编辑的文件与main分支有冲突

**解决**：
```bash
# 1. 查看冲突
git status

# 2. 手动解决冲突
# 编辑文件，移除冲突标记：
# <<<<<<< HEAD
# =======
# >>>>>>> branch

# 3. 标记为已解决
git add <resolved_files>

# 4. 在界面中继续
```

### 误删文件

**症状**：不小心删除了重要文件

**恢复**：
```bash
# 在Start Edit之后、Finish Edit之前
git checkout -- <deleted_file>

# 或者重置所有更改
git reset --hard
```

## 💡 最佳实践

### 1. 准备阶段
- 在Docker预览中仔细检查问题
- 列出需要修改的所有内容
- 确认修改符合项目规范

### 2. 编辑阶段
- 使用支持实时预览的编辑器
- 一次处理一个文件（避免混乱）
- 保持修改的最小化

### 3. 验证阶段
- Finish Edit后检查检测结果
- 在Docker预览中验证修改效果
- 确认所有修改都被检测到

### 4. 文档阶段
- 在Additional Comment中详细说明修改
- 解释为什么需要修改
- 感谢PR作者的贡献

### 示例Comment
```
Great article on deep learning! I made a few minor edits:

1. Fixed typo in the title: "Deeplearning" → "Deep Learning"
2. Corrected image paths to use Jekyll's relative_url filter
3. Added Python syntax highlighting to code blocks
4. Adjusted spacing in the conclusion section

All changes are formatting/style improvements only, no content modifications.
```

## 🎓 学习资源

### Markdown语法
- [GitHub Markdown Guide](https://guides.github.com/features/mastering-markdown/)
- [Markdown Cheatsheet](https://www.markdownguide.org/cheat-sheet/)

### Jekyll图片处理
```markdown
# 基本用法
![Alt text]({{ '/assets/images/pic.png' | relative_url }})

# 带标题
![Alt text]({{ '/assets/images/pic.png' | relative_url }})
*Figure 1: Caption text*

# HTML方式（更灵活）
<img src="{{ '/assets/images/pic.png' | relative_url }}" alt="Alt text" width="500">
```

### Git编辑技巧
```bash
# 查看修改历史
git log --oneline -10

# 查看特定文件历史
git log --follow _posts/article.md

# 比较两个版本
git diff HEAD~1 HEAD

# 查看暂存区
git diff --cached
```

## 📞 需要帮助？

- **轻微修改**：使用Edit Mode
- **重大修改**：建议让PR作者重新提交
- **不确定**：在Additional Comment中说明情况，跳过PR

---

**提示**：Edit Mode是强大的工具，但请谨慎使用。保持对PR作者工作的尊重，只修正明显的错误。
